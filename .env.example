# Copy to .env and fill in your values
# NEVER commit the actual .env file

# PostgreSQL (local Windows service)
POSTGRES_PASSWORD=your_postgres_password_here

# Moodle admin
MOODLE_ADMIN_PASSWORD=your_moodle_admin_password_here
